# for local docker-compose (this file is read by frontend/backend builds)
# do NOT use real credentials in this example file

# Backend / AWS placeholders
AWS_REGION=us-east-1
S3_BUCKET=my-megamax-bucket
AWS_ACCESS_KEY_ID=AKIAEXAMPLE
AWS_SECRET_ACCESS_KEY=secret

# Database
DATABASE_URL=postgres://postgres:password@db:5432/megamax
